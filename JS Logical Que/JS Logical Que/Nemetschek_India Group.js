// TypeScript:

// How many datatypes present in typescript
// Difference/ meaning in mutable data types and immutable data types?

// find vowels in given string, string is a case incensitive
// let input = "Hello I am Boss"
// let vowels = ['a', 'e', 'i', 'o', 'u']
// let count = 0;

// for (let val of input) {
//     if (vowels.indexOf(val.toLowerCase()) !== -1) count++
// }
// console.log(count)

// What is hoisting ?

// write code for fibonacci series:

function fib(num) {
    if (num <= 1) return num;
    return fib(num - 1) + fib(num - 2);
};
// console.log(fib(30))

// finding series:
for (let i = 0; i <= 8; i++) {
    console.log(fib(i))
}

// let a = 0, b = 1;

// for (let i = 0; i < 9; i++) {
//     console.log(a);
//     [a, b] = [b, a + b];
// }


// explain hoisting in fibonacci series code
// what is async code ?
// fibonacci series code is synch or asynch ? How ? Explain


// What is namespace ? write code like in namespace you have addition and multiplication function and access it in same file
// why using export in namespace ? Where that code is exporting (exact location) ?
// what is data types for addition and multiplication parameters ?



// Javascript:
// What is promise in javascript
// Async await

// Playwright:
// How can find page title ?
// write code for file upload
// waht is await here ?
// write code for GET api status code validations
// waht is .toBe() here ? If i get 500 insted of 200, what happens here ?

// Communication:
// how you implements shift-left approach in your automation? Suppose in login functionality, How you implement shif-left automation approach ?
// If you want to implement invoative idea ? How thats are you presenting infront of your team ?


/* 
Clear JS conscepts:

1. sync, async
2. keep answers short and effective (dont deep dive)
3. communication skill (poor)😒

*/