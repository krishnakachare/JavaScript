let add = function(a, b){//a,b here is Parameters.
  console.log(a+b);
}

add(10,20);//10,20 here is called Arguments.

//Variables in function declaration is called parameters but when we assign values to the parameters while function calling, it is called arguments.